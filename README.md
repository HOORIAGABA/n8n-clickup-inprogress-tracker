# n8n-clickup-inprogress-tracker
ClickUp → Google Sheets daily task tracker via n8n.  Logs assignments, in-progress updates (incl. project tags), deduplicates entries, and intelligently updates changed task details (dates/projects). Supports multiple teams with member/date uniqueness.
